# Docker Development Environment Variables
#
# Copy this file to .env.docker and customize for your local environment:
#   cp .env.docker.example .env.docker
#
# SECURITY WARNING: These are examples only. Generate secure values for your local setup.
# NEVER use these example values in production or commit .env.docker to git.

# PostgreSQL Database Credentials
# Generate a secure password: openssl rand -hex 16
POSTGRES_PASSWORD=your_local_dev_password_here

# NextAuth Configuration
# Generate a secure secret: openssl rand -hex 32
NEXTAUTH_SECRET=generate_with_openssl_rand_hex_32_minimum_length

# eInvoice Encryption Key
# MUST be at least 32 characters for AES-256 encryption
# Generate with: openssl rand -hex 16
EINVOICE_KEY_SECRET=your_32_character_minimum_einvoice_secret_here

# Notes:
# - These variables are loaded by docker-compose.dev.yml
# - The compose file provides fallback defaults if .env.docker is not present
# - For production deployment, use docker-compose.prod.yml with proper secrets management
