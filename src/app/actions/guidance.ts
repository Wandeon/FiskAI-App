// Re-export from canonical location for backwards compatibility with app routes
// Components should import from @/lib/actions/guidance instead
export { saveCompetenceLevel } from "@/lib/actions/guidance"
