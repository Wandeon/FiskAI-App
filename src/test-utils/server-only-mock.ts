/**
 * Mock for 'server-only' package
 *
 * The 'server-only' package is a Next.js marker that prevents client-side imports.
 * In tests, we don't need this restriction, so we export an empty module.
 */
export {}
